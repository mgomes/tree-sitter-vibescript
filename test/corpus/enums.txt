================
Enum declaration
================

enum Status
  Draft
  Published
  Archived
end

---

(program
  (enum
    name: (constant)
    member: (enum_member)
    member: (enum_member)
    member: (enum_member)))

================
Enum member access and annotations
================

enum Status
  Draft
end

def publish(status: Status) -> Status
  Status::Draft
end

---

(program
  (enum
    name: (constant)
    member: (enum_member))
  (method
    name: (identifier)
    (parameters
      (typed_parameter
        (identifier)
        (type_annotation
          (type_name
            (constant)))))
    (return_type
      (type_annotation
        (type_name
          (constant))))
    (scope_resolution
      (constant)
      (constant))))
